{
  "name": "create-portfolio-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "npm run build && next export -o out",
    "deploy": "next build && next export && touch out/.nojekyll && git add -f out/ && git commit -m \"Application Deployment Initialization.\" && git subtree push --prefix out origin page"
  },
  "dependencies": {
    "d3-ease": "^2.0.0",
    "next": "10.0.6",
    "react": "17.0.1",
    "react-circular-progressbar": "^2.0.3",
    "react-dom": "17.0.1",
    "react-flip-move": "^3.0.4",
    "react-move": "^6.4.0"
  },
  "devDependencies": {
    "babel-plugin-transform-define": "^2.0.0",
    "webpack": "^5.21.1"
  }
}
